class Question {
  final String questionText;
  final bool questionAnswer;
  //Adding a constructor by click ALT + SHIFT +ins and adding curly brace {}inside
  Question({this.questionText, this.questionAnswer});
}
